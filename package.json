{
  "name": "@glossia/l10n",
  "private": true,
  "version": "0.1.0",
  "description": "An Eleventy site and JSON Schema for the L10N.md translation-context standard.",
  "type": "module",
  "scripts": {
    "build": "npm run validate && eleventy",
    "dev": "npm run validate && eleventy --serve",
    "validate": "node scripts/validate-l10n.mjs"
  },
  "devDependencies": {
    "@11ty/eleventy": "3.1.5",
    "ajv": "8.20.0",
    "gray-matter": "4.0.3"
  }
}
